<div class="portfolio-item" data-locations="{{locations}}">
  <div class="inner">
    <img src="{{ image }}" />
    <div class="info">
      <h2>{{title}}</h2>
      <h3>{{type}}</h3>
      <p>{{description}}</p>
    </div>
  </div>
</div>
