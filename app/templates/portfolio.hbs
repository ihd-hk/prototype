<div class="portfolio-item" data-locations="{{serializedLocations}}" data-types="{{serializedTypes}}">
  <div class="inner">
    <img src="{{ image }}" />
    <div class="info">
      <h2>{{title}}</h2>
      <p>
        <span>{{location_names}}</span><br>
        <span>{{type}}</span>
      </p>
      <p class="description">{{description}}</p>
    </div>
  </div>
</div>
